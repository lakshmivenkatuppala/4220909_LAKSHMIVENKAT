git clone
---
git pull

git init

touch <filename> (to add new file)

echo "modified <filename>" >> <filename> (modifying files)

.gitignore (to ignore a same format files)

git add .

git diff(changes in modified files b/w working directory & staging)

git stash (used to revert the changes made in working directory before commiting)

git commit -m "message"

cat <filename> (displays lines)

git diff --staged (changes in modified files b/w staging & local repository)

git rm <filename>  (deletes the file from staging area & workflow directory)

git rm --cached <filename> (deletes the file only from staging)

git checkout <version> <filename> (to restore the deleted files)

git push

git status

git log

git log -oneliner

branches

git branch <branchname>

git checkout <branchname> (used to switch branches)

git diff <branch1> <branch2>

git merge <branchname> (used to merge the branches)

git branch -d <branchname> (used to delete the branch)

